

[ ] example_for_formatter.shader

[ ] shader_part1_test


() 合并cg_test和shader_part_test

() github重整

() ？实现的到底是lr1还是lalr1

() 记忆。eg: re, StringIO

() python unittest 

() wingIDE

() Git

1. 等同svn的revert

2. 从历史提交记录中移除文件

3. 等同svn的diff

() python循环引用怎么解决？

eg: from a import testA, b.py
    from b import testB, a.py

现在的work around是在用的时候才导入。


[ ] 手工填充to_code.py

[ ] 完善beautify_unity_shader的cmd
    1. 输出格式化代码文件
    2. 输出Syntax Tree

[ ] Github事项。公开这个仓库，清理仓库，增加文档

[ ] 把格式化功能接入到UnityShaderST3

[ ] UnityShaderST3发布新版本，0.3

[ ] to_scope.py

[ ] 把toScope接入到UnityShaderST3